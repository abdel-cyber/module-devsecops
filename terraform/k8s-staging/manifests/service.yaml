apiVersion: v1
kind: Service
metadata:
  name: devsecops-app-service
  namespace: tp-devsecops-staging
  labels:
    app: devsecops-app
spec:
  type: LoadBalancer
  selector:
    app: devsecops-app
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
      name: http
