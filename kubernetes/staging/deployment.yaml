apiVersion: apps/v1
kind: Deployment
metadata:
  name: devsecops-app
  namespace: tp-devsecops-staging
  labels:
    app: devsecops-app
    environment: staging
spec:
  replicas: 3  # 3 pods comme demandé
  selector:
    matchLabels:
      app: devsecops-app
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
  template:
    metadata:
      labels:
        app: devsecops-app
        environment: staging
    spec:
      containers:
      - name: devsecops-app
        image: registry.gitlab.com/abdelmouiz99/tp-devsecops-app:latest
        ports:
        - containerPort: 3000
          name: http
          protocol: TCP
        env:
        - name: NODE_ENV
          value: "staging"
        - name: PORT
          value: "3000"
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "200m"
        livenessProbe:
          httpGet:
            path: /health
            port: 3000
          initialDelaySeconds: 10
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: 3000
          initialDelaySeconds: 5
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 2
      imagePullSecrets:
      - name: gitlab-registry-secret
